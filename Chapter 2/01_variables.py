a = 1 
b = 2 # Integer variables
print (a + b) # Output: 3

c = "Alif" # String variable

d = 22.45 # Float variable


# Variable names can contain letters, digits, and underscores, but they cannot start with a digit.
# For example, "my_variable" is a valid variable name, while "2variable" is not valid.

# Variable names are case-sensitive, which means that "myVariable" and "myvariable" are considered different variables.
# It's a good practice to use descriptive variable names that indicate the purpose of the variable. For example, "age" is a better variable name than "a" when storing a person's age.

