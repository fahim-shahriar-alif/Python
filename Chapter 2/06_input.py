a = input ("What is your name? ") # The input function takes user input as a string and stores it in variable a

print("Hello " + a + ", welcome to the world of Python programming!")


b = input ("Enter number 1 : ") # The input function returns a string, so we need to convert it to an integer before performing arithmetic operations
c = input ("Enter number 2 : ")
d = int(b) + int(c) # Convert the input strings to integers before adding them
print("The sum of " + b + " and " + c + " is " + str(d))




g = int (input ("Enter number 1 : ")) # Convert the input string to an integer before storing it in variable g
h = int (input ("Enter number 2 : ")) # Convert the input string to an integer before storing it in variable h

i = g * h # Convert the input strings to integers before multiplying them
print("The product of " + str(g) + " and " + str(h) + " is " + str(i))

