
# Write a python code to calculate the square of a number that is input by the user


a = int (input ("Enter your number : ")) # Convert the input string to an integer before storing it in variable a

square = a * a # Calculate the square of the number by multiplying it by itself


print ("The square of " + str(a) + " is " + str(square)) # Output: The square of 5 is 25 (if the user inputs 5) 


b = int (input ("Enter another number : ")) # Convert the input string to an integer before storing it in variable b

b = b ** 2 # Calculate the square of the number using the exponentiation operator (**)

print ("The square of " + str(b) + " is " + str(b)) # Output: The square of 5 is 25 (if the user inputs 5) 

